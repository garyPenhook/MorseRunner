diff --git a/mac/Ini.pas b/mac/Ini.pas
index 1b2072b..5256bc3 100644
--- a/mac/Ini.pas
+++ b/mac/Ini.pas
@@ -324,7 +324,7 @@ function TSerialNRSettings.GetNR : integer;
 begin
   assert(IsValid);
   if IsValid then
-    Result := MinVal + Random(MaxVal - MinVal)
+    Result := MinVal + Random(MaxVal - MinVal + 1)
   else
     Result := 1;
 end;
@@ -352,10 +352,11 @@ begin
         'Error: ''%s'' is an invalid range.'#13 +
         'Expecting min-max values with up to 4-digits each (e.g. 100-300).',
         [ValueStr])
-    else if (Self.MinVal > 9999) or (Self.MaxVal > 9999) then
+    else if (Self.MinVal < 1) or (Self.MaxVal < 1) or
+            (Self.MinVal > 9999) or (Self.MaxVal > 9999) then
       Err := Format(
         'Error: ''%s'' is an invalid range.'#13 +
-        'Expecting range values to be less than or equal to 9999.',
+        'Expecting range values between 1 and 9999.',
         [ValueStr])
     else if (Self.MinVal > Self.MaxVal) then
       Err := Format(
diff --git a/mac/Main.pas b/mac/Main.pas
--- a/mac/Main.pas
+++ b/mac/Main.pas
@@ -470,1 +470,2 @@
-      if V > Length(ContestDefinitions) then V := 0;
+      if (V < Ord(Low(TSimContest))) or (V > Ord(High(TSimContest))) then
+        V := Ord(scWpx);
@@ -2769,1 +2770,4 @@
-  snt := TSerialNrTypes(V);
+  if (V < Ord(Low(TSerialNrTypes))) or (V > Ord(High(TSerialNrTypes))) then
+    snt := snStartContest
+  else
+    snt := TSerialNrTypes(V);
